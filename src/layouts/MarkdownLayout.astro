---
import Layout from './Layout.astro';
import MarkdownMask from './MarkdownMask.astro';

const { frontmatter, headings } = Astro.props;
---

<Layout title={frontmatter.title}>
  <MarkdownMask frontmatter={frontmatter} headings={headings}>
    <slot />
  </MarkdownMask>
</Layout>
