---
import SubPageLayout from '../layouts/SubPageLayout.astro';
import { type MarkdownInstance } from "astro";
const all_files : MarkdownInstance<Record<string,any>> [] = await Astro.glob('../content/support/*.md');
const hero : MarkdownInstance<Record<string,any>> = (await Astro.glob('../content/support/hero.md'))[0];
---
<SubPageLayout
  title="Support"
  hero={hero}
  cards={all_files}>
</SubPageLayout>