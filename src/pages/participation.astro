---
import SubPageLayout from '../layouts/SubPageLayout.astro';
import { type MarkdownInstance } from "astro";
const all_files : MarkdownInstance<Record<string,any>> [] = await Astro.glob('../content/participation/*.md');
const hero : MarkdownInstance<Record<string,any>> = (await Astro.glob('../content/participation/hero.md'))[0];
---
<SubPageLayout
  title="Participation"
  hero={hero}
  cards={all_files}>
</SubPageLayout>